Selection_Sort(A[0,...,n-1])
for j = 0 to n-2 do
    iMin = j
    for i = j + 1 to n-1 do
        if A[i] < A[iMin] then
            iMin = i
        swap(A[j],A[iMin])

A = [3,7,1,6,8,4]
#select = vybrat