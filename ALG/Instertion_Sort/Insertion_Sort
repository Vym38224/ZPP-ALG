Instertion_Sort(A[0,...,n-1])
for i = 1 in n-1 do
    t = A[i]
    j = i - 1
    while j >= 0 and A[j] > t do
        A[j+1] = A[j]
        j = j - 1
    A[j+1] = t

A = [3,7,1,6,8,4]
#instert = vložit